server:
  port: 8012
spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      # consul注册地址
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}